# config.si

# This file is meant to be a valid Simon file as well as a file taken in by shell script.
# build.sh reads this file to get config options that will help it select source
# files, among other things.
# Only simple assignments are valid in this file.

# CONFIG_ARCH should be a value from arch_list.si
# CONFIG_ARCH := ARCH_AARCH64;
CONFIG_ARCH := ARCH_X86_64;
# CONFIG_ARCH := ARCH_RISCV;
