# uart.si

uart := module {
    [[ const ]] [[ volatile ]]
    register_addr : *u32 = ~~0x09000000;

    print_string := proc (s: str) {
        loop i := 0 ; s[i] != 0; i += 1 {
            @register_addr = ~~s[i];
        }
    }
}
